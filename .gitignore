# Nunca subir credentials ni historial
*.credentials.json
.credentials.json
history.jsonl
*.jsonl

# Caché y efímeros
cache/
downloads/
paste-cache/
shell-snapshots/
session-env/
sessions/
telemetry/
file-history/
backups/
plans/
plugins/
mcp-servers/

# Per-project memory (vive en cada proyecto)
projects/

# Archivos runtime de memoria global (datos de sesión, no código)
claude/memory/session-log.txt
claude/memory/sessions.md
claude/memory/evolution-log.txt

# Helix engine — runtime (no versionar)
helix-engine/.claude-flow/data/
helix-engine/.claude-flow/sessions/
helix-engine/.claude-flow/logs/
helix-engine/.claude-flow/neural/
helix-engine/.claude-flow/metrics/
helix-engine/.claude/settings.local.json

# Runtime en raíz del repo (sesiones de trabajo sobre helix_asisten/)
.claude-flow/
.mcp.json

# Sistema
.DS_Store
Thumbs.db


# Helix Vector Memory — datos locales (nunca al repo)
claude/memory/agents/.meta/
scripts/__pycache__/
# Índices de proyectos específicos (viven solo en la máquina local)
.claude/vector-index.json
claude/memory/user-profile.md

# HELIX-COMPRESS — slices auto-generados (se regeneran en install.sh)
# No versionar: se vuelven stale con cada cambio de CLAUDE.md
claude/skills/_distilled/
claude/data/distill-meta.json
claude/data/helix-lang.jsonl
claude/data/helix-states/
labs/

# Backups internos timestamped (no versionar)
*.bak.pre-*
*.bak.pre-fable5-*
*.bak.pre-council-*
*.bak-2026*

# ── Contexto de cliente: nunca al repo público (2026-07-26) ──
# update.sh los elimina de la copia. Los que NOMBRAN a un cliente en su propio
# filename viven en .git/info/exclude (local, no versionado): listarlos acá
# publicaría el roster. La lista real está en scripts/private-patterns.local.txt.
claude/memory/topics/sessions-history.md
claude/memory/topics/‹entidad›-helix-plan-REQ-001.md
claude/memory/topics/contexto-proyecto-*.md
scripts/private-patterns.local.txt
